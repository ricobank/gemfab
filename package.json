{
  "name": "gemfab",
  "version": "0.0.2",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm run build:sol && npm run build:ts && npm run pack",
    "build:ts": "tsc",
    "build:sol": "hh compile",
    "pack": "cp dpacks/* dist/dpacks",
    "test": "npm run build && ts-mocha test/*"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "chai-as-promised": "^7.1.1",
    "debug": "^4.3.2",
    "hardhat": "^2.6.5",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "dpack": "github:dapphub/dpack"
  }
}
